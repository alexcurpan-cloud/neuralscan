# NeuralScan — dependencies, pinned (lockfile Punct 6, 19-Aug)
# Versiuni verificate local (Python 3.12) + CI (ubuntu-latest) + Railway build.
# Pin exact = build-uri reproducibile; upgrade doar intentionat.

flask==3.1.3
flask-limiter==4.1.1
gunicorn==23.0.0
pytest==9.1.1
psycopg[binary]==3.3.4
